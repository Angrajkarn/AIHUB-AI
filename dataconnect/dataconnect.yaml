specVersion: "v1"
serviceId: "aihub"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "aihub-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
